module Helpers
  module Url

    def button_to(name, options, html_options, &block)
    end

    def current_page?(options, check_parameters: false, **options_as_kwargs)
    end

    def link_to(name, options, html_options, &block)
    end

    # link_to_if
    # link_to_unless
    # link_to_unless_current

    def mail_to(email_address, name, html_options={}, &block)
    end
    
    def phone_to(phone_number, name, html_options={}, &block)
    end

    def sms_to(phone_number, name, html_options={}, &bloc)
    end

  end
end
